# Copy to .env and fill in. Never commit the real .env — hooks/pre-commit blocks it.
# NOTE: README.md says 8080 and this template says 9080. Reconcile before submitting;
# scripts/ detect the live port at runtime rather than trusting either file.
PORT=9080
